import styled from 'styled-components'
import { theme } from '../../../styles/theme'

export const S = {
  TitWrapper: styled.div`
    margin: 0 auto;
  `,
  TitWrapperText: styled.p`
    font-size: 1.6rem;
    font-weight: 700;
    color: ${theme.colors.black};
  `,
}
