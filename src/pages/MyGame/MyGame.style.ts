import styled from 'styled-components'

export const S = {
  Wrapper: styled.div`
    padding: 3rem 0 3rem 0;
    height: 100%;
    display: grid;
    grid-template-columns: 1fr 2fr 1fr;
    gap: 5rem;
  `,
}
